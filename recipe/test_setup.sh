# get the hostname of sms

# update hostname into input file to recipe, replace sms_name with hostname
# find network which will be used for provisioning -- manual
# update into file

# Update /etc/hosts with entry of sms_name
#  
