#!./common/bats/bin/bats
# -*-sh-*-

load   ./common/test_helper_functions || exit 1
source ./common/functions             || exit 1
source ./common/TEST_ENV              || exit 1
source ./LOCAL_ENV                    || exit 1

@test "[$TEST_DIR] bootstrap ($LMOD_FAMILY_COMPILER/$LMOD_FAMILY_MPI)" {
    autoreconf -f -i -Wno-portability
}

#@test "[$TEST_DIR] configure ($LMOD_FAMILY_COMPILER/$LMOD_FAMILY_MPI)" {
#    ./configure
#}
